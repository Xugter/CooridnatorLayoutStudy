<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.xugter.cooridnatorlayoutstudy">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".simpletitle.SimpleTitleActivity" />
        <activity android:name=".test.TestActivity" />
        <activity android:name=".first.FirstActivity" />
        <activity android:name=".scroll.ScrollActivity" />
        <activity android:name=".xiami.XiamiActivity" />
        <activity android:name=".base.BaseActivity1" />
        <activity
            android:name=".base.BaseActivity2"
            android:theme="@style/ToolbarTheme" />
    </application>

</manifest>